# Copia este archivo como .env y completa los valores
# NUNCA subas .env a GitHub

PORT=3000

# MongoDB Atlas (nube) - formato:
# mongodb+srv://usuario:password@cluster.mongodb.net/dragmoto
# MongoDB local:
MONGO_URI=mongodb://localhost:27017/dragmoto

# Cambia esto por una cadena larga y aleatoria en producción
JWT_SECRET=dragmoto_clave_secreta_2025
